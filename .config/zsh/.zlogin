# .zlogin is sourced for login shells, after .zshrc.
#
# Note: the session (Hyprland) is launched by greetd, NOT from here. The old
# Xorg startx + prime-switch block was removed when migrating to Wayland +
# greetd; do not re-add startx here.
