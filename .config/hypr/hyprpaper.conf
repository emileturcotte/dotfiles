# Hyprpaper — Émile Turcotte
# https://wiki.hypr.land/Hypr-Ecosystem/hyprpaper/
#
# Drop a wallpaper image at the path below, then uncomment the two lines.
# Until then, the background falls back to the solid Nord color set by
# Hyprland's misc:background_color (no error, just no image).

# preload = ~/.config/hypr/wallpaper.png
# wallpaper = ,~/.config/hypr/wallpaper.png

ipc = on
splash = false
